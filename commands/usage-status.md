---
description: Show this week's Claude Code usage for the current project and account.
---

Run the usage-limiter status report by invoking:

```
node ${CLAUDE_PLUGIN_ROOT}/bin/commands/status.js
```

Then relay its stdout to the user verbatim.
