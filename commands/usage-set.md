---
description: Set the current project's weekly usage cap (percent or USD).
allowed-tools: Bash
argument-hint: <value> e.g. 20% or $50 or 50usd
---

Run `node ${CLAUDE_PLUGIN_ROOT}/bin/commands/set.js $ARGUMENTS` and relay its stdout verbatim.
