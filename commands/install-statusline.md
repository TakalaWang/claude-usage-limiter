---
description: Install the usage-limiter statusline into ~/.claude/settings.json.
allowed-tools: Bash
---

Run `node ${CLAUDE_PLUGIN_ROOT}/bin/commands/install-statusline.js` and relay its stdout verbatim.
