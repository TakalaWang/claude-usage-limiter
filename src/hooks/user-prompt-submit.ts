// UserPromptSubmit hook: reads JSON from stdin, decides whether to block.
// Block shape: { "decision": "block", "reason": "..." } on stdout, exit 0.
// Stub — implementation pending.

export async function main(): Promise<void> {
  // pending — allow by default
  process.exit(0);
}
